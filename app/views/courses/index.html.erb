<% if current_admin != nil %>
<h2 class="searchTitle">All Courses</h2>
<small>Search By Name</small>
<%= form_tag("/courseresults", method: "get") do %>
<%= text_field_tag(:q) %> <br>
<%=submit_tag("Search") %>
<%end%>
<ol>
<% @courses.each do |c| %>
<li> <%= link_to "Landing Page for #{c.name}", user_course_path(c.user_id, c) %> <br>
  <% if current_admin != nil %>
    <%= link_to 'Delete Course', user_course_path(c.user_id, c), data: {confirm: 'This will delete the course.'}, :method=> :delete %>
  </li>
<%end%>
<%end%>
</ol>
<%else%>
<p>You do not have permission to view this page</p>
<%end%>
